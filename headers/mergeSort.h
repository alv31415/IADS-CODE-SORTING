#include <cmath>
#include "helpers.h"

using namespace std;

vector<int> merge (vector<int>, vector<int>);

vector<int> mergeSortIdx (vector<int>, int, int);

vector<int> mergeSort(vector<int>);

void testMergeSort();